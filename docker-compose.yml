version: '3'
services:
 django:
      build:
       context: ./django/
       dockerfile: Dockerfile
      container_name: Django-app
      volumes:
       - ./django/src/:/app/
      ports:
       - 8005:8005
      tty: true
      command: "gunicorn -b 0.0.0.0:8005 movie_app.wsgi:application"

